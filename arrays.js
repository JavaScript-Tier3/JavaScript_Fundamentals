var shoppingList = ["butter", "milk", "eggs"];
console.log(shoppingList);

shoppingList = ["Eggs", "Butter", "Apples"]; // you can replace data in an array
var eggs = shoppingList[0]; // you can assign new data to an index
shoppingList[1] = "Cheese"; // same here

var listLenght = shoppingList.length()

shoppingList.push("butter");
shoppingList.pop();